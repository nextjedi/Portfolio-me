version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: portfolio-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Optional: mount for development hot reload
    # volumes:
    #   - ./dist/portfolio-redesign/browser:/usr/share/nginx/html:ro
